const TODO_STATUS = Object.freeze({
  TODO: 'TODO',
  IN_PROGRESS: 'IN_PROGRESS',
  DONE: 'DONE',
});

const THEME = Object.freeze({
  LIGHT: 'LIGHT',
  DARK: 'DARK',
});

module.exports = { TODO_STATUS, THEME };
